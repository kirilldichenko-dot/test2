export function createDurakGame({ root }) {
  root.innerHTML =
    "<div class='game-info'>🃏 Дурак будет добавлен позже. В этой версии доступна демо‑архитектура Web App и примеры шахмат/шашек.</div>";
  return { reset() {} };
}

